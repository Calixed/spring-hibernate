# eager-and-lazy-fetch_type-hibernate-java-demo
- simple java project that demonstrate one-to-many hibernate
- it needs to run the mysql script first to build the database schema and set up the tables


